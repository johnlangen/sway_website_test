/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://swaywellnessspa.com', // ✅ Use your real site URL
    generateRobotsTxt: true,                // ✅ This auto-generates /robots.txt
    sitemapSize: 7000,                      // Optional: chunking for large sites
  };
  